export enum EventTypeEnum {
  OFFLINE = "offline",
  ONLINE = "online",
}

export enum FormElementType {
  INPUT = "input",
  SELECT = "select",
  UPLOAD = "upload",
  DATE = "date",
  GRID = "grid",
  PASSWORD = "password",
  TEXTAREA = "textarea",
  CHECKBOX = "checkbox",
}
